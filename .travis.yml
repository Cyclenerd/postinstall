language: bash
sudo: required
os:
  - linux
  - osx
before_script:
  - curl -f "https://raw.githubusercontent.com/lehmannro/assert.sh/v1.1/assert.sh" -o assert.sh
script:
  - sudo bash -c 'echo "Travis $TRAVIS"'
  - sudo bash -c 'echo "Travis OS $TRAVIS_OS_NAME"'
  - sudo bash postinstall.sh -t server
  - bash tests.sh